/**
 * Auth feature module
 *
 * Usage:
 *   import { useAuth, useUserProfile, signInWithCredentials } from "@/features/auth";
 */

// Hooks
export * from "./hooks";

// Components will be re-exported here as they are migrated
// export * from "./components";
